{
  "name": "cicd-demo-app",
  "version": "1.0.0",
  "description": "Demo Node.js app for AWS CodePipeline + CodeBuild",
  "main": "index.js",
  "scripts": {
    "build": "echo 'Building project... ✅'",
    "test": "echo 'Running tests... ✅' && exit 0"
  },
  "dependencies": {
    "lodash": "^4.17.21"
  }
}
